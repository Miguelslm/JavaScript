let numeros = [1, 2, 3, 4, 5];
let soma = 0;

for (let i = 0; i < numeros.length; i++) {
    soma += numeros[i]
}
 
console.log("A soma de todos os elemtos do array é:", soma);

let numero = 2;
while(numero <= 10) {
    if(numero % 2 ===0 ) {
        console.log(numero);
    }
    numero++;
}

