//outra forma de fazer funçoes

const mediaAluno = (n1 = 0, n2 = 0, rep = 0) => {
    let media = (n1 + n2 + rep) / 3;
    return media0;
};

console.log(`Nota do aluno: ${mediaAluno(8, 5, 7).toFixed(1)}`)

